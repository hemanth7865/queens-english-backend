// Placeholder lessons list - update with real data as needed
export type Lesson = {
  id: string;
  number: string;
  type: string;
};

const lessons: Lesson[] = [
  // Add your lessons here
  // Example:
  // { id: "lesson-01", number: "01", type: "grammar" },
];

export default lessons;
