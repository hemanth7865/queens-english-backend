# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  - feature-release

pool:
  vmImage: ubuntu-latest

variables:
  ${{ if startsWith(variables['Build.SourceBranch'], 'refs/heads/') }}:
    BRANCH_NAME: $[ replace(variables['Build.SourceBranch'], 'refs/heads/', '') ]

  ${{ if startsWith(variables['Build.SourceBranch'], 'refs/pull/') }}:
    BRANCH_NAME: $[ replace(variables['System.PullRequest.SourceBranch'], 'refs/heads/', '') ]

steps:
  - task: SSH@0
    displayName: "Connect Staging Server, And Run Deployment Scripts"
    inputs:
      sshEndpoint: "QE_PORTAL STAGING"
      runOptions: "commands"
      commands: |
        echo "Clean Local Changes"
        cd /home/azureuser/qe-backend && git add . && git stash
        echo "Download Updates"
        cd /home/azureuser/qe-backend && git pull &> /home/azureuser/git-pull.txt && cat /home/azureuser/git-pull.txt
        echo "Checkout Branch $(BRANCH_NAME)"
        cd /home/azureuser/qe-backend && git checkout $(BRANCH_NAME) &> /home/azureuser/git-checkout.txt && cat /home/azureuser/git-checkout.txt
        cd /home/azureuser/qe-backend && git pull &> /home/azureuser/git-pull.txt && cat /home/azureuser/git-pull.txt 
        echo "Install dependencies"
        cd /home/azureuser/qe-backend/schedule-api && yarn install &> /home/azureuser/be-install.txt && cat /home/azureuser/be-install.txt
        echo "Run Migrations"
        cd /home/azureuser/qe-backend/schedule-api && yarn migrate
        echo "Install dependencies"
        cd /home/azureuser/qe-backend/qe-admin && yarn install &> /home/azureuser/fe-install.txt && cat /home/azureuser/fe-install.txt
        echo "Build UI"
        cd /home/azureuser/qe-backend/qe-admin && yarn build &> /home/azureuser/build.txt && cat /home/azureuser/build.txt 
        rm -rf /var/www/html/qe-admin
        cd /home/azureuser/qe-backend/qe-admin && mv dist /var/www/html/qe-admin
        rm -rf /var/www/html/qe-admin-ui && cp -r /var/www/html/qe-admin /var/www/html/qe-admin-ui
        pm2 restart all
      readyTimeout: "20000"
      # Don't Show Error Message If Command Fail
      # failOnStdErr: false
